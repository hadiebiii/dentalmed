# راهنمای استفاده از کاتالوگ واژگان دندان‌پزشکی

## ساختار پوشه‌ها

فایل‌ها را دقیقاً با این ساختار در مخزن GitHub قرار دهید:

```text
repository-root/
├── index.html
└── lessons/
    ├── lesson-001.md
    ├── lesson-002.md
    ├── lesson-003.md
    ├── ...
    └── lesson-097.md
```

نام پوشه باید `lessons` و نام فایل‌ها باید سه‌رقمی باشد.

## نحوه نمایش

- صفحه اصلی هیچ عنوانی مانند `LESSON 001`، `Vocabulary Module 001` یا جملهٔ «۱۰ واژه...» نشان نمی‌دهد.
- تنها ده واژه درس انتخاب‌شده نمایش داده می‌شود.
- با انتخاب هر واژه، درسنامه کامل همان واژه باز می‌شود.
- اطلاعات جداشده با ویرگول یا سمی‌کالن به‌صورت بولت‌پوینت یا برچسب رنگی نمایش داده می‌شوند.
- مثال‌ها، تعریف‌ها، خانواده واژه، کاربرد تخصصی، شبکه معنایی، بازیابی فعال، آزمون و مرور فاصله‌دار از Markdown استخراج می‌شوند.
- بخش فاقد داده به‌طور کامل حذف می‌شود و کارت خالی ساخته نمی‌شود.
- پاسخ تست در حالت اولیه مخفی است.

## ساختار قابل تشخیص Markdown

هر مدخل باید با این الگو آغاز شود:

```markdown
### V0001. Hermetic
```

فیلدها با این قالب خوانده می‌شوند:

```markdown
- **Part of Speech:** adj.
- **IPA:** /hermetic/
- **معادل فارسی:** کاملاً airtight، نفوذناپذیر
- **ترجمه مدرس:** کاملاً airtight، نفوذناپذیر; مثال: نمونه‌ها در یک محفظه کاملاً نفوذناپذیر نگهداری شدند.
```

برای خروجی بولت‌پوینتی بهتر، موارد را با یکی از این جداکننده‌ها جدا کنید:

```text
،
,
؛
;
```

نمونه:

```markdown
- **ترجمه مدرس:** نفوذناپذیر؛ کاملاً بسته؛ مثال: نمونه‌ها در یک محفظه نفوذناپذیر نگهداری شدند.
```

در سایت به این شکل نمایش داده می‌شود:

- نفوذناپذیر
- کاملاً بسته
- مثال: نمونه‌ها در یک محفظه نفوذناپذیر نگهداری شدند.

## اجرای محلی

به علت استفاده از `fetch` برای خواندن Markdown، فایل را مستقیماً با `file://` باز نکنید.

در پوشه پروژه اجرا کنید:

```bash
python3 -m http.server 8080
```

سپس باز کنید:

```text
http://127.0.0.1:8080
```

## GitHub Pages

1. `index.html` را در ریشه مخزن قرار دهید.
2. پوشه `lessons` را کنار آن قرار دهید.
3. در GitHub وارد `Settings > Pages` شوید.
4. منبع انتشار را روی شاخه اصلی و پوشه `/root` قرار دهید.
5. منتظر تکمیل انتشار بمانید.

## تغییر رنگ‌ها

تمام رنگ‌ها در ابتدای `index.html` و بخش `:root` تعریف شده‌اند. برای تغییر پوسته فقط متغیرهای زیر را ویرایش کنید:

```css
--purple
--blue
--green
--yellow
--pink
--orange
--teal
```

## فونت

هیچ فونت یا CDN خارجی استفاده نشده است. سایت از فونت‌های نصب‌شده روی دستگاه استفاده می‌کند و در نبود آن‌ها به `Segoe UI`، `Tahoma` و `Arial` برمی‌گردد.

## تلفظ

تلفظ از `window.speechSynthesis` مرورگر استفاده می‌کند و هیچ فایل صوتی یا API خارجی فراخوانی نمی‌شود.
