# راهنمای کاتالوگ واژگان دندان‌پزشکی

## ساختار پوشه‌ها

```text
repository-root/
├── index.html
└── lessons/
    ├── index.json        ← نمایه جست‌وجو (خودکار ساخته می‌شود)
    ├── lesson-001.md
    ├── lesson-002.md
    └── … lesson-097.md
```

نام پوشه باید `lessons` و شماره فایل‌ها سه‌رقمی باشد.

## ساختار هر واژه

هر مدخل دقیقاً شش بخش دارد و هیچ بخش دیگری نمایش داده نمی‌شود:

| بخش | محتوا |
|---|---|
| معنی | معادل‌های فارسی به‌صورت برچسب + یک بند توضیح |
| تعریف و مفهوم | تعریف دقیق انگلیسی + اثر انگشت معنایی |
| جملات | یک جمله مستند از رفرنس + سه جمله برای درک مفهوم، همراه توضیح فارسی |
| کاربرد تخصصی | کاربرد بالینی واژه در یک بند |
| کلمات با املای مشابه | واژه‌های قابل اشتباه با معنی کوتاه |
| روایت آزمونی | مترادف/متضاد، چکیده تحلیل، استراتژی تست‌زنی و سه تست شبیه‌ساز |

بخشی که داده نداشته باشد به‌طور کامل حذف می‌شود و کارت خالی ساخته نمی‌شود.
پاسخ تست‌ها در حالت اولیه پنهان است.

## قالب Markdown

```markdown
### V0008. Inconsequential

**نقش:** صفت · **POS:** adj. · **حوزه:** Prosthodontics

#### معنی

**معادل‌های اصلی**

- بی‌اهمیت
- ناچیز

**ترجمه و توضیح**

توضیح فارسی در یک بند.

#### تعریف و مفهوم

**تعریف دقیق**

`Not important or significant; trivial.`

**اثر انگشت معنایی**

وجه ممیز معنایی واژه.

#### جملات

**جمله مبتنی بر رفرنس** — پروتز ثابت (Shillingburg)

> Restorations will require some intraoral adjustment, but it should be ==hl-emerald|inconsequential== if the restoration is fabricated carefully.

توضیح فارسی جمله.

**سه جمله برای درک مفهوم واژه**

1. The slight shade mismatch was deemed ==hl-amber|inconsequential== by the patient.

   توضیح فارسی جمله.

#### کاربرد تخصصی

بند کاربرد بالینی.

#### کلمات با املای مشابه

- **Inconsequent** (صفت) — فاقد نتیجه منطقی، نامربوط

#### روایت آزمونی

**مترادف‌های آزمونی**

- Trivial

**متضادهای کلیدی**

- Paramount

**چکیده تحلیل آزمونی**

- بولت اول
- بولت دوم

**استراتژی تست‌زنی**

بند استراتژی.

**تست 1**

Since the pit affected neither retention nor hygiene, the finding was judged .......... .

A. paramount
B. inconsequential
C. substantial
D. mandatory

**پاسخ:** B

توضیح فارسی پاسخ.
```

### هایلایت واژه هدف

نحو `==رنگ|متن==` واژه هدف را رنگی می‌کند. رنگ‌های موجود:

| کلید | رنگ |
|---|---|
| `hl-emerald` | سبز کم‌رنگ |
| `hl-amber` | زرد کم‌رنگ |
| `hl-rose` | صورتی کم‌رنگ |
| `hl-sky` | آبی کم‌رنگ |

## بازتولید محتوا

محتوای درس‌ها از روی داده‌های ساختاریافته ساخته می‌شود و مستقیماً ویرایش نمی‌شود:

```bash
cd /home/hadi/dental
python3 scripts/10_build_inventory.py     # استخراج فهرست واژه‌ها
python3 scripts/11_mine_sentences.py      # استخراج جملات واقعی از ۲۳ رفرنس
python3 scripts/12_index_exam.py          # نمایه بانک سؤال دستیاری
python3 scripts/13_lookalikes.py          # نامزدهای کلمات هم‌املا
python3 scripts/14_build_packs.py         # ساخت بسته ورودی هر درس
python3 scripts/21_audit.py               # اعتبارسنجی محتوای تولیدشده
python3 scripts/20_render_lessons.py      # تولید lesson-*.md و index.json
python3 scripts/22_assemble_course.py     # تولید خروجی‌های سطح پروژه
```

## اجرای محلی

به علت استفاده از `fetch`، فایل را با `file://` باز نکنید:

```bash
cd dentalmed
python3 -m http.server 8080
```

سپس `http://127.0.0.1:8080` را باز کنید.

## GitHub Pages

1. `index.html` را در ریشه مخزن قرار دهید.
2. پوشه `lessons` را کنار آن بگذارید.
3. در `Settings > Pages` منبع انتشار را روی شاخه اصلی و پوشه `/root` تنظیم کنید.

## تغییر پوسته

همه رنگ‌ها در `:root` ابتدای `index.html` تعریف شده‌اند. برای تغییر پوسته فقط
این متغیرها را ویرایش کنید:

```css
--canvas  --paper  --ink  --line
--teal  --indigo  --amber  --rose  --sky  --moss
```

## فونت و تلفظ

فونت فارسی Vazirmatn از CDN بارگذاری می‌شود و در نبود اینترنت به فونت‌های
سیستمی برمی‌گردد. جملات انگلیسی با فونت سریف و تعریف‌ها با فونت مونواسپیس
نمایش داده می‌شوند. تلفظ از `window.speechSynthesis` مرورگر استفاده می‌کند و
هیچ فایل صوتی یا API خارجی فراخوانی نمی‌شود.

## میان‌برهای صفحه‌کلید

| کلید | کار |
|---|---|
| `/` | رفتن به جست‌وجو |
| `←` / `→` | واژه بعدی / قبلی |
| `Esc` | بستن فهرست یا جست‌وجو |
